# This GitHub Action workflow was auto-generated by the `rdme` cli on :[](timestamp)
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # You can add more branches by duplicating this line below
      # for each branch you'd like to add!
      - :[](branch)

jobs:
  simple:
    name: My First GitHub Action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout GitHub Action
        uses: actions/checkout@v3

      - name: Run `:[](command)` command
        uses: readmeio/rdme@:[](rdmeVersion)
        with:
          rdme: :[](command) :[](optsString)
