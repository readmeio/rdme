name: GitHub Action Test

on: [push]

jobs:
  action-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0
        with:
          repository: kanadgupta/metrotransit-nextrip-oas

      - name: Run GitHub Action command test
        uses: ./
        with:
          rdme: validate
